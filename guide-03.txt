tutorial folder :03
1. install nodemon di dev
2. add script in package.json
3. add nodemon.json
4. remove entry point main in package json
5. create file .babelrc

alasan menggunakan .babelrc :
Babel transpiles your code to vanilla JavaScript. When you use an upcoming
JavaScript language feature, which hasn't been introduced in Node.js, you can still 
use the feature in your source code. Babel makes sure that Node.js understands it.


6. yarn add dotenv => jika kita ingin set value config atau
environment variable tanpa show di source code
